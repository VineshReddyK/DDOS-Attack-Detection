apiVersion: v1
kind: Service
metadata:
  name: ddos-detection-api
  namespace: default
  labels:
    app: ddos-detection-api
spec:
  type: LoadBalancer
  selector:
    app: ddos-detection-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
